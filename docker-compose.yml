services:
  dashboard:
    build: .
    container_name: drugans-dashboard
    restart: unless-stopped
    ports:
      - "49195:49195"
    env_file:
      - .env
    volumes:
      - db-data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=49195
      - DB_PATH=/app/data/dashboard.db

volumes:
  db-data:
